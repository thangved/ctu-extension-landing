pipeline {
    agent any

    stages {
        stage('Install') {
            steps {
                npm 'install'
            }
        }

        stage('Build') {
            steps {
                npm 'run build'
            }
        }
    }
}